<!--
  Este archivo lo usa Tiny Tapeout para generar el "datasheet" público de tu
  proyecto (la página que ve la gente en tinytapeout.com). Rellena las
  secciones de abajo y borra las que no apliquen (ej. si no usas hardware
  externo, borra esa sección completa).

  Puedes incluir imágenes en esta misma carpeta docs/ y referenciarlas desde
  el markdown (ya tienes imagenes.svg / imagenes.rar aquí). Cada imagen debe
  pesar menos de 512 KB, y el total de todas las imágenes menos de 1 MB.
-->

## How it works

<!-- TODO: explica en tus palabras cómo funciona tu CPU: registros A/B, ALU
     (suma/resta), IR, PC, la FSM de control de 3 estados (cargar IR ->
     ejecutar -> incrementar PC), y el set de instrucciones (NOP, ADD, SUB,
     LOAD A, LOAD B, STORE A, STORE B). Este es buen lugar para referenciar
     imagenes.svg si es un diagrama del datapath. -->

## How to test

<!-- TODO: explica cómo alguien probaría tu chip una vez fabricado: qué
     valores poner en los pines de entrada, qué instrucción cargar, qué
     esperar ver en las salidas. Esto es lo que luego se traduce al
     testbench cocotb que exige el flujo oficial de Tiny Tapeout (distinto
     de tu tb/Testbench.sv actual, que es para simulación local con
     iverilog/gtkwave). -->

## External hardware

<!-- TODO: si no usas ningún hardware externo (LEDs, displays, PMODs, etc.),
     puedes borrar esta sección completa. -->

---
